---
phase: 06-configuration-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - openrag/config/config.py
  - .hydra_config/config.yaml
  - openrag/routers/utils.py
  - openrag/config/test_config.py
  - openrag/routers/test_utils.py
autonomous: true

must_haves:
  truths:
    - "Hydra configuration loads without emitting version_base warnings"
    - "Config values are identical before and after version_base change"
    - "Legacy partition prefix 'ragondin-' still functions but emits DeprecationWarning"
    - "Current partition prefix 'openrag-' works without emitting any warnings"
    - "All 93 existing tests continue passing"
  artifacts:
    - path: "openrag/config/config.py"
      provides: "Hydra config loading with version_base=None"
      contains: "version_base=None"
    - path: ".hydra_config/config.yaml"
      provides: "Clean defaults list without TODO warning comment"
      contains: "_self_"
    - path: "openrag/routers/utils.py"
      provides: "Legacy partition prefix deprecation warning"
      contains: "warnings.warn"
    - path: "openrag/config/test_config.py"
      provides: "Tests verifying config loads without warnings and values unchanged"
    - path: "openrag/routers/test_utils.py"
      provides: "Tests verifying deprecation warning for legacy prefix"
  key_links:
    - from: "openrag/config/config.py"
      to: ".hydra_config/config.yaml"
      via: "initialize_config_dir loads config.yaml"
      pattern: "version_base=None"
    - from: "openrag/routers/utils.py"
      to: "openrag/consts.py"
      via: "LEGACY_PARTITION_PREFIX constant used in warning message"
      pattern: "consts\\.LEGACY_PARTITION_PREFIX"
---

<objective>
Fix Hydra configuration version warning suppression (DEBT-03) and add deprecation warnings for legacy partition prefix backward compatibility (DEBT-04).

Purpose: Remove technical debt from configuration. The Hydra version_base="1.1" was set as a quick fix to silence warnings without proper review. The legacy "ragondin-" partition prefix has an XXX comment marking it for removal but no deprecation path. This plan resolves both by using proper Hydra forward-compatible settings and adding standard Python DeprecationWarning for the legacy prefix.

Output: Updated config.py, config.yaml, utils.py, plus new test files verifying both changes.
</objective>

<execution_context>
@/home/paul/.claude/get-shit-done/workflows/execute-plan.md
@/home/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-configuration-cleanup/06-RESEARCH.md
@openrag/config/config.py
@openrag/routers/utils.py
@openrag/consts.py
@.hydra_config/config.yaml
@pytest.ini
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Hydra version_base and add config verification tests</name>
  <files>openrag/config/config.py, .hydra_config/config.yaml, openrag/config/test_config.py</files>
  <action>
1. In `openrag/config/config.py`:
   - Change `version_base="1.1"` to `version_base=None` on line 20
   - Remove the TODO comment on line 19 (`# TODO: I set the version base to 1.1 to silence the warning message, review how we want to handle versioning`)
   - Add a brief comment explaining the choice: `# version_base=None uses current Hydra version defaults (forward compatible)`

2. In `.hydra_config/config.yaml`:
   - Remove the TODO comment from line 2. Change:
     `- _self_ # TODO: Silences the hydra version migration warning (PLEASE REVIEW FOR BREAKING CHANGES)`
     to:
     `- _self_`

3. Create `openrag/config/test_config.py` with tests:
   - `test_config_loads_successfully`: Call `load_config()` and assert it returns a non-None config object with expected top-level keys (llm, embedder, vectordb, paths, loader).
   - `test_config_critical_values_unchanged`: Call `load_config()` and verify critical config values match expected defaults from config.yaml:
     - `config.llm.temperature == 0.1`
     - `config.embedder.provider == "openai"`
     - `config.vectordb.hybrid_search == True` (note: YAML true becomes Python bool)
     - `config.loader.image_captioning == True`
   - `test_config_no_hydra_version_warnings`: Use `warnings.catch_warnings(record=True)` context manager with `warnings.simplefilter("always")`, call `load_config()`, then assert no warnings contain "version_base" in their message string. Do NOT use `pytest.warns(None)` as it has compatibility issues; use the stdlib warnings approach.

   Important: The test file must import using absolute imports per project convention:
   ```python
   from config import load_config
   ```
   Tests must be regular functions (not async) since load_config is synchronous.
  </action>
  <verify>
Run `uv run pytest openrag/config/test_config.py -v` and confirm all 3 tests pass.
Run `uv run pytest` and confirm all tests pass (93 existing + 3 new = 96).
  </verify>
  <done>
config.py uses version_base=None with no TODO comment. config.yaml has clean _self_ line with no TODO. Three new tests verify config loading works correctly, values are unchanged, and no Hydra version warnings are emitted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add deprecation warning for legacy partition prefix</name>
  <files>openrag/routers/utils.py, openrag/routers/test_utils.py</files>
  <action>
1. In `openrag/routers/utils.py`:
   - Add `import warnings` at the top of the file (with the other stdlib imports)
   - In the `get_partition_name` function (around line 294), replace:
     ```python
     if model_name.startswith(consts.LEGACY_PARTITION_PREFIX):
         # XXX - This is for backward compatibility, but should eventually be removed
         partition_prefix = consts.LEGACY_PARTITION_PREFIX
     ```
     with:
     ```python
     if model_name.startswith(consts.LEGACY_PARTITION_PREFIX):
         warnings.warn(
             f"The partition prefix '{consts.LEGACY_PARTITION_PREFIX}' is deprecated "
             f"and will be removed in a future version. "
             f"Please update your model names to use '{consts.PARTITION_PREFIX}' instead. "
             f"Example: '{consts.LEGACY_PARTITION_PREFIX}mypartition' -> '{consts.PARTITION_PREFIX}mypartition'",
             DeprecationWarning,
             stacklevel=2,
         )
         partition_prefix = consts.LEGACY_PARTITION_PREFIX
     ```
   - Do NOT add a specific version number or date to the deprecation message (we don't know the release schedule). Use "in a future version" which is the standard Python pattern when timeline is uncertain.

2. Create `openrag/routers/test_utils.py` with tests:
   - `test_legacy_prefix_emits_deprecation_warning`: Use `pytest.warns(DeprecationWarning, match="deprecated")` to verify that calling the partition prefix detection logic with a model name starting with "ragondin-" triggers a DeprecationWarning. Since `get_partition_name` is async and calls `ray.get_actor`, mock the vectordb actor to avoid Ray dependency. Use `unittest.mock.patch` to mock `routers.utils.get_vectordb` to return a MagicMock, and mock `ray.get_actor` if needed. The test should be marked `@pytest.mark.asyncio`.
   - `test_current_prefix_no_deprecation_warning`: Verify that a model name starting with "openrag-" does NOT trigger a DeprecationWarning. Use `warnings.catch_warnings(record=True)` with `warnings.simplefilter("always")`, call the function (with vectordb mocked), and assert no DeprecationWarning was captured. Mock the vectordb so `partition_exists.remote` returns a truthy AsyncMock. Mark as `@pytest.mark.asyncio`.

   Important notes for mocking:
   - `get_partition_name` calls `get_vectordb()` which does `ray.get_actor("Vectordb", namespace="openrag")`. Mock `routers.utils.get_vectordb` to return a MagicMock.
   - The function calls `await vectordb.partition_exists.remote(partition)` so mock it as an AsyncMock returning True.
   - For the legacy prefix test, the function will succeed if partition_exists returns True and the user has access (pass `is_admin=True` and `user_partitions=[]`).
   - Use absolute imports: `from routers.utils import get_partition_name`
  </action>
  <verify>
Run `uv run pytest openrag/routers/test_utils.py -v` and confirm both tests pass.
Run `uv run pytest` and confirm all tests pass (96 from Task 1 + 2 new = 98).
  </verify>
  <done>
Legacy partition prefix "ragondin-" emits a DeprecationWarning with clear migration instructions when used. Current prefix "openrag-" produces no warnings. XXX comment removed and replaced with proper deprecation mechanism. Two new tests verify the warning behavior.
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest` - All tests pass (93 existing + 5 new)
2. `uv run ruff check openrag/config/test_config.py openrag/routers/test_utils.py openrag/config/config.py openrag/routers/utils.py` - No lint errors
3. Verify no TODO or XXX comments remain in modified files:
   - `grep -n "TODO\|XXX" openrag/config/config.py` returns nothing
   - `grep -n "TODO" .hydra_config/config.yaml` returns nothing
   - `grep -n "XXX" openrag/routers/utils.py` returns nothing
4. Verify version_base=None is in config.py: `grep "version_base=None" openrag/config/config.py`
5. Verify warnings.warn is in utils.py: `grep "warnings.warn" openrag/routers/utils.py`
</verification>

<success_criteria>
- Hydra config loads with version_base=None (no warning suppression hack)
- config.yaml has clean _self_ without TODO comment
- Legacy "ragondin-" prefix emits DeprecationWarning with migration instructions
- Current "openrag-" prefix works without any warnings
- All TODO/XXX debt markers removed from modified files
- 5 new tests verify the changes
- All 93 original tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-configuration-cleanup/06-01-SUMMARY.md`
</output>
