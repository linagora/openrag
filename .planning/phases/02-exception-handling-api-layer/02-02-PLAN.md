---
phase: 02-exception-handling-api-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - openrag/routers/indexer.py
autonomous: true

must_haves:
  truths:
    - "File upload endpoints catch OSError/IOError for disk operations before generic Exception"
    - "Ray actor calls catch RayTaskError and ValueError (actor not found) specifically"
    - "Task management endpoints distinguish between task-not-found vs unexpected errors"
    - "Generic error responses do not expose file paths or internal stack traces"
  artifacts:
    - path: "openrag/routers/indexer.py"
      provides: "Tiered exception handling for file operations and task management"
      min_lines: 650
      contains: "except OSError"
  key_links:
    - from: "openrag/routers/indexer.py"
      to: "components/indexer/utils/files.py"
      via: "save_file_to_disk OSError handling"
      pattern: "except OSError.*save.*file"
    - from: "openrag/routers/indexer.py"
      to: "ray.ObjectRef"
      via: "RayTaskError catching on task operations"
      pattern: "except.*RayTaskError"
---

<objective>
Replace 5 bare exception handlers in the indexer router with tiered exception handling for file operations and task management.

Purpose: File uploads and task state management require specific error handling for disk I/O failures, Ray task errors, and validation failures. Generic exception catching masks the root cause and exposes internal details.

Output: openrag/routers/indexer.py with specific exception handling for file operations, task queries, and task lifecycle management
</objective>

<execution_context>
@/home/paul/.claude/get-shit-done/workflows/execute-plan.md
@/home/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-exception-handling-api-layer/02-RESEARCH.md
@openrag/routers/indexer.py
@openrag/components/indexer/utils/files.py
@openrag/utils/exceptions/base.py
@openrag/utils/exceptions/vectordb.py
</context>

<tasks>

<task type="auto">
  <name>Replace file upload exception handlers</name>
  <files>openrag/routers/indexer.py</files>
  <action>
Replace 2 bare exception handlers in file upload endpoints with tiered handling for disk I/O errors:

**Lines 145-148** (add_file endpoint - save_file_to_disk):
- Catch HTTPException first and re-raise
- Catch OSError specifically (covers IOError, PermissionError, disk full errors)
  - Log with: logger.exception("Failed to save file to disk", file_id=file_id)
  - Raise HTTPException(500, detail="Failed to save uploaded file due to storage error")
- Catch Exception with generic message
  - Log with: logger.exception("Unexpected error saving file", file_id=file_id)
  - Raise HTTPException(500, detail="An unexpected error occurred while saving the file")

**Lines 260-263** (update_file endpoint - save_file_to_disk):
- Same pattern as add_file above
- Use logger.bind(file_id=file_id, partition=partition) for structured logging context

Implementation notes:
- OSError is the base class for IOError, PermissionError, FileNotFoundError, etc.
- Never include str(e) in user-facing error messages for generic Exception
- Keep structured logging with file_id and partition context where available
  </action>
  <verify>
1. Check OSError handling exists: `grep -n "except OSError" openrag/routers/indexer.py` (should show 2 matches around lines 145, 260)
2. Verify no path exposure: `grep "detail=.*str(e)" openrag/routers/indexer.py` (should show NO matches in updated sections)
3. Run linting: `uv run ruff check openrag/routers/indexer.py`
  </verify>
  <done>
File upload endpoints catch OSError for disk operations with user-friendly error messages. Generic Exception handlers do not expose file paths or internal error details.
  </done>
</task>

<task type="auto">
  <name>Replace task management exception handlers</name>
  <files>openrag/routers/indexer.py</files>
  <action>
Replace 3 bare exception handlers in task management endpoints with tiered handling for Ray errors:

**Lines 537-540** (get_task_error endpoint):
- Catch HTTPException and re-raise
- Catch RayTaskError with specific message: "Failed to retrieve task error information"
- Catch KeyError with HTTPException(404, detail="Task error information not found")
- Catch Exception with generic message: "An unexpected error occurred while retrieving task error"

**Lines 585-588** (get_task_logs endpoint):
- Catch HTTPException and re-raise
- Catch OSError/IOError for log file read failures
  - HTTPException(500, detail="Failed to read task log file")
- Catch json.JSONDecodeError for malformed log entries
  - HTTPException(500, detail="Task log file format is invalid")
- Catch Exception with generic message: "An unexpected error occurred while retrieving task logs"

**Lines 618-621** (cancel_task endpoint):
- Catch HTTPException and re-raise
- Catch RayTaskError with message: "Failed to cancel task execution"
- Catch ValueError with HTTPException(404, detail="Task not found")
- Catch Exception with generic message: "An unexpected error occurred while cancelling the task"

Implementation notes:
- Import json.JSONDecodeError at top of file if not already imported
- Ray actor "not found" errors manifest as ValueError, not RayTaskError
- Task state manager uses Ray internally, so RayTaskError is expected for task operation failures
- Use structured logging: logger.bind(task_id=task_id, partition=partition)
  </action>
  <verify>
1. Check Ray error handling: `grep -n "except RayTaskError" openrag/routers/indexer.py` (should show matches in task endpoints)
2. Check ValueError handling: `grep -n "except ValueError" openrag/routers/indexer.py` (should show match in cancel_task)
3. Verify no bare handlers remain: `grep -n "except Exception:" openrag/routers/indexer.py` (should return NO matches)
4. Run: `uv run pytest openrag/ -k indexer -v` (if tests exist)
  </verify>
  <done>
Task management endpoints catch RayTaskError, ValueError, OSError, and JSONDecodeError specifically before falling back to generic Exception. Error messages are user-friendly and do not expose task internals.
  </done>
</task>

</tasks>

<verification>
## Post-Task Verification

After completing both tasks:

1. **No bare exception handlers remain:**
   ```bash
   grep -n "except Exception:" openrag/routers/indexer.py
   # Should return NO matches
   ```

2. **File I/O errors are specific:**
   ```bash
   grep -A2 "save_file_to_disk" openrag/routers/indexer.py | grep -A5 "except"
   # Should show OSError handler
   ```

3. **Ray errors are caught:**
   ```bash
   grep -B5 -A2 "RayTaskError" openrag/routers/indexer.py
   # Should show specific handlers in task management endpoints
   ```

4. **No internal details exposed:**
   ```bash
   grep "detail=.*str(e)" openrag/routers/indexer.py
   grep "detail=f.*{.*e" openrag/routers/indexer.py
   # Both should return NO matches
   ```

5. **All tests pass:**
   ```bash
   uv run pytest openrag/ -v
   # All 93 tests should pass
   ```
</verification>

<success_criteria>
1. File upload endpoints catch OSError specifically for disk operations
2. Task management endpoints catch RayTaskError and ValueError for Ray operations
3. All 5 exception handlers use tiered catching with specific types first
4. Generic HTTP 500 responses do not expose file paths or internal errors
5. All 93 existing tests continue passing
6. No ruff linting errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/02-exception-handling-api-layer/02-02-SUMMARY.md`
</output>
