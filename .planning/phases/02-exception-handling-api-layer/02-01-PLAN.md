---
phase: 02-exception-handling-api-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - openrag/routers/openai.py
autonomous: true

must_haves:
  truths:
    - "Chat completion endpoints catch specific exception types before falling back to generic Exception"
    - "Streaming responses handle asyncio.CancelledError separately to detect client disconnection"
    - "Generic HTTP 500 errors do not expose internal error details to clients"
    - "HTTPException instances are re-raised unchanged to preserve status codes"
  artifacts:
    - path: "openrag/routers/openai.py"
      provides: "Tiered exception handling for chat completions and completions endpoints"
      min_lines: 350
      contains: "except asyncio.CancelledError:"
  key_links:
    - from: "openrag/routers/openai.py"
      to: "utils/exceptions/base.py"
      via: "OpenRAGError subclass catching"
      pattern: "except OpenRAGError"
    - from: "openrag/routers/openai.py streaming generator"
      to: "asyncio.CancelledError"
      via: "client disconnection detection"
      pattern: "except asyncio.CancelledError"
---

<objective>
Replace 7 bare exception handlers in the OpenAI-compatible router with tiered exception handling that catches specific exception types.

Purpose: Improve error diagnostics and prevent exposing internal error details while maintaining OpenAI API compatibility. The OpenAI router handles chat completions and completions endpoints with streaming support, making it critical for proper error signaling to clients.

Output: openrag/routers/openai.py with specific exception handling for streaming and non-streaming endpoints
</objective>

<execution_context>
@/home/paul/.claude/get-shit-done/workflows/execute-plan.md
@/home/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-exception-handling-api-layer/02-RESEARCH.md
@openrag/routers/openai.py
@openrag/utils/exceptions/base.py
@openrag/utils/exceptions/vectordb.py
@openrag/utils/exceptions/embeddings.py
@openrag/components/ray_utils.py
</context>

<tasks>

<task type="auto">
  <name>Replace exception handlers in chat_completions endpoint (streaming and non-streaming)</name>
  <files>openrag/routers/openai.py</files>
  <action>
Replace 4 bare exception handlers in the `/v1/chat/completions` endpoint with tiered exception handling:

**Lines 173-177** (get_partition_name error - currently re-raises):
- Keep re-raise behavior but add specific catch for HTTPException first
- Catch VDBPartitionNotFound specifically
- Add generic Exception handler with structured error message

**Lines 180-184** (ragpipe.chat_completion error):
- Catch HTTPException first and re-raise
- Catch OpenRAGError and let global handler convert to JSON
- Catch RayTaskError and TaskCancelledError with specific error messages
- Catch Exception with generic "unexpected error" message without exposing details

**Lines 208-220** (streaming generator error):
- Add asyncio.CancelledError as FIRST handler (log clean disconnect, return cleanly)
- Add RayTaskError and TaskCancelledError handlers (yield SSE error event with code "RAY_TASK_ERROR")
- Add OpenRAGError handler (yield SSE error event with e.code and e.message)
- Keep existing Exception handler but use generic message "An unexpected error occurred during streaming" instead of str(e)

**Lines 229-233** (non-streaming response error):
- Catch HTTPException first and re-raise
- Catch OpenRAGError and let global handler convert
- Catch Exception with generic error message

Implementation notes:
- Use pattern from research: HTTPException first, then specific types, then Exception
- For streaming: errors must be yielded as SSE data events, not raised as HTTP exceptions
- Log all exceptions with appropriate level (warning for expected errors, exception for unexpected)
- Never include str(e) in user-facing messages for generic Exception catches
  </action>
  <verify>
1. Run: `uv run ruff check openrag/routers/openai.py` (no new issues)
2. Search for remaining bare handlers: `grep -n "except Exception" openrag/routers/openai.py | grep -v "# " | head -5` (should show lines 296, 303, 318 only - completions endpoint)
3. Verify streaming exception order: `grep -A5 "except asyncio.CancelledError" openrag/routers/openai.py` (should appear before other exception handlers)
  </verify>
  <done>
Chat completions endpoint catches asyncio.CancelledError, RayTaskError, TaskCancelledError, OpenRAGError, and HTTPException specifically before falling back to generic Exception handler. Streaming error events do not expose internal error details. All handlers log appropriately.
  </done>
</task>

<task type="auto">
  <name>Replace exception handlers in completions endpoint</name>
  <files>openrag/routers/openai.py</files>
  <action>
Replace 3 bare exception handlers in the `/v1/completions` endpoint with tiered exception handling:

**Lines 296-300** (get_partition_name error):
- Same pattern as chat_completions: HTTPException first, VDBPartitionNotFound, then Exception

**Lines 303-307** (ragpipe.completions error):
- Catch HTTPException and re-raise
- Catch OpenRAGError (let global handler convert)
- Catch RayTaskError and TaskCancelledError with specific messages
- Catch Exception with generic message

**Lines 318-322** (non-streaming response error):
- Catch HTTPException and re-raise
- Catch OpenRAGError
- Catch Exception with generic message

Implementation notes:
- Use same tiered pattern: HTTPException → specific exceptions → Exception
- This endpoint does NOT support streaming according to code review, so no asyncio.CancelledError handling needed
- Keep error messages generic for Exception catch-all
- Log with structured logging using logger.bind() where file_id or partition context exists
  </action>
  <verify>
1. Search for remaining bare handlers: `grep -n "except Exception:" openrag/routers/openai.py` (should return NO matches - all converted)
2. Run: `uv run pytest openrag/ -k openai -v` (if router tests exist)
3. Verify no HTTPException re-wrapping: `grep -A3 "except HTTPException" openrag/routers/openai.py` (should show "raise" on next line)
  </verify>
  <done>
Completions endpoint has tiered exception handling with HTTPException preservation, specific exception type catching, and generic Exception fallback with sanitized error messages. No bare exception handlers remain in openai.py.
  </done>
</task>

</tasks>

<verification>
## Post-Task Verification

After completing both tasks:

1. **No bare exception handlers remain:**
   ```bash
   grep -n "except Exception:" openrag/routers/openai.py
   # Should return NO matches
   ```

2. **Streaming error handling is correct:**
   ```bash
   grep -B2 -A10 "async def stream_response" openrag/routers/openai.py | grep "except"
   # Should show: asyncio.CancelledError, RayTaskError/TaskCancelledError, OpenRAGError, Exception (in that order)
   ```

3. **HTTPException is preserved:**
   ```bash
   grep -A1 "except HTTPException" openrag/routers/openai.py
   # All should show immediate "raise"
   ```

4. **No internal details exposed:**
   ```bash
   grep "detail=str(e)" openrag/routers/openai.py
   # Should return NO matches
   grep "detail=f.*{.*e" openrag/routers/openai.py
   # Should return NO matches
   ```

5. **All tests pass:**
   ```bash
   uv run pytest openrag/ -v
   # All 93 tests should pass
   ```
</verification>

<success_criteria>
1. All 7 exception handlers in openai.py catch specific exception types before Exception
2. Streaming generator catches asyncio.CancelledError FIRST to handle client disconnection
3. HTTPException instances are re-raised unchanged throughout the file
4. Generic HTTP 500 responses use sanitized error messages without str(e)
5. All 93 existing tests continue passing
6. No ruff linting errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/02-exception-handling-api-layer/02-01-SUMMARY.md`
</output>
