---
phase: 02-exception-handling-api-layer
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - openrag/routers/tools.py
  - openrag/routers/utils.py
  - openrag/routers/actors.py
  - openrag/routers/extract.py
autonomous: true

must_haves:
  truths:
    - "Tool execution endpoint catches HTTPException before cleanup logic in finally block"
    - "Actor management endpoints distinguish between actor-not-found vs actor-kill-failed errors"
    - "Model availability check catches httpx.HTTPError for LLM API failures"
    - "Document extraction endpoint catches VDBError subclasses for chunk lookup failures"
  artifacts:
    - path: "openrag/routers/tools.py"
      provides: "Tiered exception handling with cleanup in finally block"
      min_lines: 140
      contains: "except HTTPException"
    - path: "openrag/routers/actors.py"
      provides: "Ray actor lifecycle error handling"
      min_lines: 140
      contains: "except ValueError"
    - path: "openrag/routers/utils.py"
      provides: "LLM availability check with HTTP error handling"
      min_lines: 270
      contains: "except httpx.HTTPError"
    - path: "openrag/routers/extract.py"
      provides: "Vector DB error handling for chunk retrieval"
      min_lines: 70
      contains: "except VDBError"
  key_links:
    - from: "openrag/routers/tools.py"
      to: "finally block cleanup"
      via: "Exception handling preserves cleanup logic"
      pattern: "finally:.*unlink"
    - from: "openrag/routers/actors.py"
      to: "ray.get_actor"
      via: "ValueError catching for actor not found"
      pattern: "except ValueError.*actor.*not.*found"
    - from: "openrag/routers/utils.py"
      to: "httpx.AsyncClient"
      via: "httpx.HTTPError for LLM API failures"
      pattern: "except httpx.HTTPError"
---

<objective>
Replace 6 bare exception handlers across tools, utils, actors, and extract routers with tiered exception handling.

Purpose: These smaller routers handle specialized operations: tool execution with file cleanup, Ray actor lifecycle management, LLM availability checks, and document chunk extraction. Each requires specific error handling for their domain.

Output: 4 router files with specific exception handling appropriate to their operations
</objective>

<execution_context>
@/home/paul/.claude/get-shit-done/workflows/execute-plan.md
@/home/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-exception-handling-api-layer/02-RESEARCH.md
@openrag/routers/tools.py
@openrag/routers/utils.py
@openrag/routers/actors.py
@openrag/routers/extract.py
@openrag/utils/exceptions/base.py
@openrag/utils/exceptions/vectordb.py
</context>

<tasks>

<task type="auto">
  <name>Replace exception handlers in tools and utils routers</name>
  <files>openrag/routers/tools.py, openrag/routers/utils.py</files>
  <action>
**tools.py - Lines 124-141 (execute_tool endpoint):**

The current pattern with cleanup in finally block is GOOD - preserve it. Enhance the try/except before finally:

- Line 124: Catch HTTPException first and re-raise
- Add OSError catch for file read operations with message: "Failed to process uploaded file"
- Add json.JSONDecodeError for tool parameter parsing (if applicable)
- Keep Exception handler with generic message: "An unexpected error occurred during tool execution"
- Line 137: Keep existing cleanup try/except in finally block unchanged (research confirms this is correct pattern)

**utils.py - Lines 267-270 (check_llm_model_availability):**

This function makes HTTP requests to LLM endpoints. Replace bare exception handler:

- Add httpx.HTTPError catch specifically:
  - Log: logger.warning("LLM model availability check failed", model=model, error=str(e))
  - Raise HTTPException(503, detail="LLM service is unavailable")
- Add httpx.TimeoutException:
  - Log: logger.warning("LLM model availability check timed out", model=model)
  - Raise HTTPException(504, detail="LLM service timed out")
- Keep Exception handler with generic message: "Failed to check LLM model availability"

Implementation notes:
- Import httpx at top of utils.py if not already imported
- tools.py cleanup logic in finally should remain unchanged (nested try/except is appropriate)
- Use structured logging with model parameter context in utils.py
  </action>
  <verify>
1. Check httpx error handling: `grep -n "except httpx" openrag/routers/utils.py` (should show HTTPError and TimeoutException)
2. Verify finally block unchanged: `grep -A8 "finally:" openrag/routers/tools.py` (should show original nested try/except for cleanup)
3. Run linting: `uv run ruff check openrag/routers/tools.py openrag/routers/utils.py`
  </verify>
  <done>
Tools router catches specific exceptions while preserving cleanup logic in finally block. Utils router catches httpx errors for LLM availability checks with appropriate 503/504 status codes.
  </done>
</task>

<task type="auto">
  <name>Replace exception handlers in actors and extract routers</name>
  <files>openrag/routers/actors.py, openrag/routers/extract.py</files>
  <action>
**actors.py - Three exception handlers:**

Lines 66-69 (list_actors endpoint):
- Catch RayTaskError: "Failed to retrieve actor list"
- Catch Exception with generic message: "An unexpected error occurred while listing actors"

Lines 119-122 (kill_actor endpoint - currently has good pattern at line 117):
- The ValueError handler at line 117 is CORRECT - keep it
- Enhance line 119 Exception handler:
  - Catch RayTaskError first: "Failed to kill actor"
  - Keep Exception handler but sanitize message to: "An unexpected error occurred while killing actor"
  - REMOVE the f-string that exposes error details: detail=f"Failed to kill actor {actor_name}: {e!s}"

Lines 139-142 (restart_actor endpoint):
- Catch ValueError first: HTTPException(404, detail="Actor not found")
- Catch RayTaskError: "Failed to restart actor"
- Catch Exception with generic message: "An unexpected error occurred while restarting actor"

**extract.py - Lines 70-73 (get_chunk_by_id endpoint):**
- Catch HTTPException and re-raise
- Catch VDBError specifically (base class for all VDB errors):
  - Let the global handler in api.py convert it to JSON response
- Catch RayTaskError: "Failed to retrieve document chunk"
- Catch Exception with generic message: "An unexpected error occurred while retrieving chunk"

Implementation notes:
- Ray actor "not found" errors are ValueError, not RayTaskError (confirmed in research)
- VDBError has proper to_dict() method for structured responses
- Remove detail=f"...{e!s}" patterns that expose internal error details
- Use logger.bind(actor_name=actor_name) for structured logging in actors.py
  </action>
  <verify>
1. Check ValueError handling: `grep -n "except ValueError" openrag/routers/actors.py` (should show 2 matches)
2. Check VDBError handling: `grep -n "except VDBError" openrag/routers/extract.py` (should show 1 match)
3. Verify no error detail exposure: `grep 'detail=f.*{.*e' openrag/routers/actors.py` (should return NO matches)
4. Check all files: `grep -n "except Exception:" openrag/routers/tools.py openrag/routers/utils.py openrag/routers/actors.py openrag/routers/extract.py` (should return NO matches)
  </verify>
  <done>
Actor management endpoints catch ValueError for actor-not-found and RayTaskError for operation failures. Extract endpoint catches VDBError for chunk lookup failures. No error responses expose internal details.
  </done>
</task>

</tasks>

<verification>
## Post-Task Verification

After completing both tasks:

1. **No bare exception handlers remain in all 4 files:**
   ```bash
   grep -n "except Exception:" openrag/routers/tools.py openrag/routers/utils.py openrag/routers/actors.py openrag/routers/extract.py
   # Should return NO matches
   ```

2. **HTTPException preservation across all files:**
   ```bash
   grep -A1 "except HTTPException" openrag/routers/tools.py openrag/routers/extract.py
   # All should show immediate "raise"
   ```

3. **No internal details exposed:**
   ```bash
   grep "detail=f.*{.*e" openrag/routers/tools.py openrag/routers/utils.py openrag/routers/actors.py openrag/routers/extract.py
   grep "detail=.*str(e)" openrag/routers/tools.py openrag/routers/utils.py openrag/routers/actors.py openrag/routers/extract.py
   # Both should return NO matches
   ```

4. **All tests pass:**
   ```bash
   uv run pytest openrag/ -v
   # All 93 tests should pass
   ```

5. **No linting errors:**
   ```bash
   uv run ruff check openrag/routers/
   # Should show no new issues
   ```
</verification>

<success_criteria>
1. Tools router preserves cleanup logic in finally block while catching specific exceptions
2. Utils router catches httpx.HTTPError and TimeoutException for LLM availability checks
3. Actors router catches ValueError for actor-not-found and RayTaskError for lifecycle operations
4. Extract router catches VDBError for chunk retrieval operations
5. All 6 exception handlers use tiered catching with specific types before Exception
6. No error responses expose internal error details or stack traces
7. All 93 existing tests continue passing
8. No ruff linting errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/02-exception-handling-api-layer/02-03-SUMMARY.md`
</output>
